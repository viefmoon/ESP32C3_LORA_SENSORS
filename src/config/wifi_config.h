#pragma once

// WiFi credentials
const char* WIFI_SSID = "your_ssid";
const char* WIFI_PASSWORD = "your_password";

// NTP Configuration
constexpr bool ENABLE_TIME_SYNC = true;  // Set to false to disable NTP sync 